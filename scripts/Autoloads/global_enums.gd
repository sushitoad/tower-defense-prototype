
class_name GlobalEnums
#maybe just give it a class name

enum BeaconType { BASIC, CHARGE, HEARTFIRE, LIGHTBURN, PRISM, SUN }

#I think this needs a level of abstraction, so its creating a reference to the var type
